<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sattaapp">

    <application
        android:name=".VoicemeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.PlayMatka.PlayMatkaActivity"></activity>
        <activity android:name=".ui.Result.ResultActivity" />
        <activity android:name=".ui.History.HistoryActivity" />
        <activity android:name=".ui.Withdraw.WithdrawActivity" />
        <activity android:name=".ui.Contact.ContactUsActivity" />
        <activity android:name=".ui.Homepage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.SingleLocationPage" />
        <activity android:name=".ui.CreateGroupTagActivity" />
        <activity android:name=".ui.Homepage.LoginScreenActivity" />
        <activity android:name=".ui.Homepage.LocationPageActivity" />
    </application>

</manifest>